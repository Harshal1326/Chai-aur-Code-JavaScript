"use strict" // treat all JS code as newer version

// alert(3 + 3) // we are using node.js here not brower so it will not work

// code should have high readability

console.log("Harshal")

let name = "Harshal" // String Data Type
let age = 18  // int
let isLogedIn = false // boolean

// Data Types list => number -> 2 to power 53, bigint, String, boolean, null -> standalone value, undefined, symbol -> to find unique

// Object 

console.log(typeof null);  // o/p: object
console.log(typeof undefined);  // o/p: undefined